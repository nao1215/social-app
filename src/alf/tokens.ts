/**
 * デザイントークン定義ファイル
 *
 * このファイルはデザインシステム全体で使用される基本的なデザイントークン
 * （定数値）を定義します。
 *
 * デザイントークンとは:
 * UI全体で一貫性を保つための基本的な値（色、サイズ、余白など）のこと。
 * トークンを使用することで、デザイン変更時に一箇所を修正するだけで
 * アプリ全体に反映できます。
 *
 * Go開発者向け補足:
 * - これらの定数はGoのconst定義のようなもの
 * - as constを使用することで、TypeScriptの型システムで厳密な型推論が可能
 * - スペーシングシステムは8の倍数を基準とした一貫性のあるデザイン
 */

// プラットフォーム検出ユーティリティをインポート
import {isAndroid} from '#/platform/detection'

/**
 * 文字間隔（レタースペーシング）
 *
 * Androidでは文字間を少し広げることで可読性を向上させます。
 * iOSではデフォルト（0）を使用。
 *
 * Go開発者向け: 条件付きコンパイルのようなプラットフォーム固有の定数
 */
export const TRACKING = isAndroid ? 0.1 : 0

/**
 * 一時的な色定義
 *
 * 開発中に使用される仮の色。
 * 最終的には正式なパレットに置き換える予定の色。
 *
 * Go開発者向け: 開発用の定数グループ、本番では使用しない想定
 */
export const color = {
  temp_purple: 'rgb(105 0 255)',      // 一時的な紫色
  temp_purple_dark: 'rgb(83 0 202)',  // 一時的な暗い紫色
} as const

/**
 * スペーシングトークン
 *
 * 余白（margin/padding）とギャップ（gap）に使用される値。
 * 8の倍数を基準とした一貫性のあるスケール。
 *
 * 命名規則:
 * - _2xs: extra extra small (2px)
 * - xs: extra small (4px)
 * - sm: small (8px)
 * - md: medium (12px)
 * - lg: large (16px)
 * - xl: extra large (20px)
 * - _2xl: extra extra large (24px)
 * - _3xl, _4xl, _5xl: さらに大きいサイズ
 *
 * Go開発者向け:
 * - これはマジックナンバーを避けるための定数マップ
 * - 8pxを基準単位とすることで、視覚的に調和したレイアウトを実現
 * - _で始まる名前（_2xs等）は、数字で始められないための命名規則
 */
export const space = {
  _2xs: 2,   // 極小余白（2px）
  xs: 4,     // 小余白（4px）
  sm: 8,     // 基準余白（8px）- 最も頻繁に使用
  md: 12,    // 中余白（12px）
  lg: 16,    // 大余白（16px）
  xl: 20,    // 特大余白（20px）
  _2xl: 24,  // 超特大余白（24px）
  _3xl: 28,  // さらに大きい余白（28px）
  _4xl: 32,  // 非常に大きい余白（32px）
  _5xl: 40,  // 最大余白（40px）
} as const

/**
 * フォントサイズトークン
 *
 * テキストのフォントサイズ定義。
 * タイポグラフィの階層を表現するためのスケール。
 *
 * 使用例:
 * - _2xs, xs: キャプション、メタ情報
 * - sm: ボディテキスト（小）
 * - md: デフォルトボディテキスト
 * - lg, xl: サブヘッディング
 * - _2xl, _3xl, _4xl, _5xl: 見出し（大きさ順）
 *
 * Go開発者向け:
 * - アクセシビリティのため、最小サイズでも読みやすい10px以上を使用
 * - 各サイズ間には十分な差をつけて、明確な階層を実現
 */
export const fontSize = {
  _2xs: 10,  // 極小テキスト（10px）- メタ情報、補足
  xs: 12,    // 小テキスト（12px）- キャプション
  sm: 14,    // 小ボディテキスト（14px）
  md: 16,    // デフォルトボディテキスト（16px）- 最も読みやすいサイズ
  lg: 18,    // 大テキスト（18px）- 小見出し
  xl: 20,    // 特大テキスト（20px）
  _2xl: 22,  // 見出し（22px）
  _3xl: 26,  // 中見出し（26px）
  _4xl: 32,  // 大見出し（32px）
  _5xl: 40,  // 最大見出し（40px）- ヒーローテキスト
} as const

/**
 * 行の高さ（line-height）トークン
 *
 * テキストの行間を制御する値。
 * 読みやすさとレイアウトの密度のバランスを取ります。
 *
 * - none (1): 行間なし - タイトルや単一行テキスト用
 * - normal (1.5): 標準行間 - 通常のボディテキスト用
 * - relaxed (1.625): ゆったり行間 - 長文テキスト用
 *
 * Go開発者向け: フォントサイズに対する相対的な比率（倍率）
 */
export const lineHeight = {
  none: 1,         // 行間なし（フォントサイズと同じ高さ）
  normal: 1.5,     // 標準行間（フォントサイズの1.5倍）
  relaxed: 1.625,  // ゆったり行間（フォントサイズの1.625倍）
} as const

/**
 * ボーダー半径（border-radius）トークン
 *
 * 角の丸みを定義する値。
 * UIコンポーネントの角を丸くするために使用。
 *
 * - full (999): 完全な円形（ボタン、アバター等）
 * - その他: 段階的な丸み
 *
 * Go開発者向け:
 * - 999は十分大きな値で、どんなサイズの要素でも完全な円形になる
 * - 小さい値ほど角ばった印象、大きい値ほど丸い印象
 */
export const borderRadius = {
  _2xs: 2,   // 極小丸み（2px）- 微妙な丸み
  xs: 4,     // 小丸み（4px）
  sm: 8,     // 基準丸み（8px）- カード、ボタン等
  md: 12,    // 中丸み（12px）
  lg: 16,    // 大丸み（16px）
  full: 999, // 完全円形 - アバター、ピル型ボタン等
} as const

/**
 * フォントウェイト（font-weight）トークン
 *
 * 実際にロードされるInterフォントファイルに対応する太さ。
 * 使用可能な太さのみを定義しています。
 *
 * - normal (400): 通常テキスト
 * - medium (500): やや太いテキスト（ボタンラベル等）
 * - bold (600): 太字（見出し、強調等）
 * - heavy (800): 極太（大見出し、重要な強調等）
 *
 * Go開発者向け:
 * - 文字列で定義するのはCSSの仕様に従うため
 * - 100〜900の範囲で定義可能だが、実際にロードしたフォントの太さのみ使用
 */
export const fontWeight = {
  normal: '400',  // 通常の太さ
  medium: '500',  // 中程度の太さ
  bold: '600',    // 太字
  heavy: '800',   // 極太
} as const

/**
 * グラデーション定義
 *
 * アバター、ボタン、背景等で使用されるグラデーション。
 * 各グラデーションは色の配列とホバー時の色を持ちます。
 *
 * 構造:
 * - values: [位置(0-1), 色]の配列（線形グラデーションの色経由点）
 * - hover_value: ホバー時に使用する単色
 *
 * グラデーション種類:
 * - primary: プライマリブルーグラデーション
 * - sky: 空のようなブルーグラデーション
 * - midnight: 深夜のような暗いブルー
 * - sunrise: 日の出のような暖色グラデーション
 * - sunset: 夕焼けのようなピンク〜オレンジ
 * - summer: 夏のようなオレンジ〜イエロー
 * - nordic: 北欧風のブルー〜グリーン
 * - bonfire: 焚き火のようなダーク〜オレンジ
 *
 * Go開発者向け:
 * - 配列の最初の数値は位置（0=開始、1=終了）
 * - Linear Gradientで使用: 開始色から終了色へスムーズに変化
 * - hover_valueは、マウスホバー時のフォールバック色（グラデーション非対応時）
 */
export const gradients = {
  // プライマリグラデーション（ブルー系）
  primary: {
    values: [
      [0, '#054CFF'],    // 開始: 深いブルー
      [0.4, '#1085FE'],  // 中間1: 明るいブルー
      [0.6, '#1085FE'],  // 中間2: 明るいブルー（維持）
      [1, '#59B9FF'],    // 終了: ライトブルー
    ],
    hover_value: '#1085FE',  // ホバー時の単色
  },
  // 空グラデーション
  sky: {
    values: [
      [0, '#0A7AFF'],   // 開始: ブルー
      [1, '#59B9FF'],   // 終了: ライトブルー
    ],
    hover_value: '#0A7AFF',
  },
  // 深夜グラデーション
  midnight: {
    values: [
      [0, '#022C5E'],   // 開始: ダークブルー
      [1, '#4079BC'],   // 終了: ミディアムブルー
    ],
    hover_value: '#022C5E',
  },
  // 日の出グラデーション
  sunrise: {
    values: [
      [0, '#4E90AE'],   // 開始: ブルー
      [0.4, '#AEA3AB'], // 中間1: グレイッシュパープル
      [0.8, '#E6A98F'], // 中間2: ピーチ
      [1, '#F3A84C'],   // 終了: オレンジ
    ],
    hover_value: '#AEA3AB',
  },
  // 夕焼けグラデーション
  sunset: {
    values: [
      [0, '#6772AF'],   // 開始: パープル
      [0.6, '#B88BB6'], // 中間: ライトパープル
      [1, '#FFA6AC'],   // 終了: ピンク
    ],
    hover_value: '#B88BB6',
  },
  // 夏グラデーション
  summer: {
    values: [
      [0, '#FF6A56'],   // 開始: レッド
      [0.3, '#FF9156'], // 中間: オレンジ
      [1, '#FFDD87'],   // 終了: イエロー
    ],
    hover_value: '#FF9156',
  },
  // 北欧グラデーション
  nordic: {
    values: [
      [0, '#083367'],   // 開始: ダークブルー
      [1, '#9EE8C1'],   // 終了: ミントグリーン
    ],
    hover_value: '#3A7085',
  },
  // 焚き火グラデーション
  bonfire: {
    values: [
      [0, '#203E4E'],   // 開始: ダークブルーグレー
      [0.4, '#755B62'], // 中間1: モーヴ
      [0.8, '#CD7765'], // 中間2: コーラル
      [1, '#EF956E'],   // 終了: ピーチオレンジ
    ],
    hover_value: '#755B62',
  },
} as const
